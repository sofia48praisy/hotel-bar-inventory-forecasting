# What to upload

| Form field | File |
|---|---|
| Take-Home Coding Project (general project upload) | `krystal_ball_submission.zip` in the kristball folder, if ZIP is accepted |
| 1) Clean, well-documented Python Notebook | `inventory_forecasting_standalone.ipynb` in this folder |
| 2) Short video demo, 3-5 minutes | Your recorded screen-and-voice video, under 10 MB |
| 3) Short write-up | `business_report.pdf` in this folder |

The last screenshot includes repeated video wording followed by the write-up questions. Match its upload to the write-up; use the dedicated video field for your recording. If the upload picker rejects a type, check its supported extensions rather than renaming the file's extension.

The standalone notebook includes the original CSV and implementation. Reviewers do not need the project ZIP to execute it, but do need the Python packages listed in the first cell. Running it writes a `krystal_ball_run` directory beside the notebook. It includes executed results.

Practise using `bar_inventory_project/video_script/video_narration_and_interview_guide.md` before recording. All written/code deliverables are prepared; the required personal video is still outstanding. No form has been submitted.
